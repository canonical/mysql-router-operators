summary: test_subordinate_charms_new.py
environment:
  TEST_MODULE: test_subordinate_charms_new.py
execute: |
  # TODO: Uncomment when ubuntu-advantage and landscape-client get built for Ubuntu 26.04
  # tox run -e integration -- "tests/integration/integration/$TEST_MODULE" --alluredir="$SPREAD_TASK/allure-results"
  exit 0
artifacts:
  - allure-results
backends:
  - -lxd-vm  # Requires CI secrets
