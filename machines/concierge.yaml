juju:
  model-defaults:
    logging-config: <root>=INFO; unit=DEBUG
providers:
  juju:
    channel: "3.6/stable"
    agent-version: "3.6.9"
  lxd:
    enable: true
    bootstrap: true
host:
  snaps:
    jhack:
      channel: latest/edge
      connections:
        - jhack:dot-local-share-juju snapd
